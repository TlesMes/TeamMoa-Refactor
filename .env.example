# ================================================================
# TeamMoa Environment Variables
# ================================================================
#
# 사용 방법:
# 1. 개발 환경: cp .env.example .env (개발 섹션 사용)
# 2. 프로덕션: cp .env.example .env.production (프로덕션 섹션 사용)
#
# ================================================================

# ================================================================
# 공통 설정 (Common Settings)
# ================================================================

# Django Secret Key
# 개발: 임시 키 사용 가능
# 프로덕션: 아래 명령으로 새 키 생성
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your_secret_key_here

# Email Settings (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password

# Database Settings
DB_ENGINE=django.db.backends.mysql
DB_NAME=teammoa_db
DB_USER=teammoa_user
DB_PASSWORD=your_database_password
DB_PORT=3306

# OAuth 2.0
GOOGLE_OAUTH_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret-here
GITHUB_OAUTH_CLIENT_ID=your-github-client-id
GITHUB_OAUTH_CLIENT_SECRET=your-github-client-secret


# ================================================================
# 환경 설정 안내
# 아래 설정은 개발 또는 프로덕션 환경 중 하나만 활성화하여 사용
# - 개발 환경: 로컬 또는 Docker 개발용
# - 프로덕션 환경: 실제 서비스 배포용
# ================================================================


# ================================================================
# 개발 환경 설정 (Development Environment)
# ================================================================

DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Site Settings (for django.contrib.sites and email links)
SITE_DOMAIN=localhost:8000
SITE_NAME=TeamMoa Local

# Database Host
# 로컬 MySQL 사용: localhost
# Docker 사용: db
DB_HOST=localhost

# ================================================================
# 프로덕션 환경 설정 (Production Environment)
# ================================================================

# DEBUG=False
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# SITE_DOMAIN=yourdomain.com
# SITE_NAME=TeamMoa
# DB_HOST=db
# DB_ROOT_PASSWORD=your_secure_root_password
# DB_CONN_MAX_AGE=600

# Redis Settings (Django Channels - WebSocket)
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=your_secure_redis_password

# Security Settings
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True

# CORS Settings
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Admin URL (보안 강화)
# ADMIN_URL=admin/

# Sentry (에러 추적)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
